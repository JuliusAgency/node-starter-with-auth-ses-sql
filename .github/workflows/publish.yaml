name: Publish To GitHub

on:
  workflow_dispatch:
    branches: [main]
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
    
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # - name: Extract metadata (tags, labels) for Docker
      #   id: meta
      #   uses: docker/metadata-action@v4
      #   with:
      #     images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      #     tags: |
      #       type=raw,value=latest,enable=${{ endsWith(GitHub.ref, 'main') }}
      #     flavor: |
      #       latest=true

      - name: Build and push
        run: |
          docker build . --build-arg=READ_FROM_REGISTRY=${{ secrets.READ_FROM_REGISTRY }} --tag ghcr.io/juliusagency/${{ env.IMAGE_NAME }}:${{github.run_id}}
          docker push ghcr.io/juliusagency/${{ env.IMAGE_NAME }}:${{github.run_id}}
          
          # docker build . --build-arg=READ_FROM_REGISTRY=${{ secrets.READ_FROM_REGISTRY }} --tag ghcr.io/${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{github.run_id}}
          # docker push ghcr.io/${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{github.run_id}}
          # ghcr.io/ghcr.io/JuliusAgency/node-starter-with-auth-ses-sql

        # uses: docker/build-push-action@v3
        # with:
        #   context: .
        #   file: ./Dockerfile
        #   build-args: |
        #     "READ_FROM_REGISTRY=${{ secrets.READ_FROM_REGISTRY }}"
        #   push: ${{ github.ref_type == 'tag' }}
        #   # push: true
        #   tags: ${{ steps.meta.outputs.tags }}
        #   labels: ${{ steps.meta.outputs.labels }}
        #   outputs: type=image,name=target
